<!DOCTYPE html>
<html>
  <head>
    <title>Bookmark Manager</title>
  </head>

  <body>
  	<h1>Bookmarks app</h1>
    <% if current_user %>
      Welcome <%= current_user.email %>
    <% end %>
    <%= yield %> 
  </body>
</html>